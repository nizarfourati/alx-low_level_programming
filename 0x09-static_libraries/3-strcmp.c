#include "main.h"
/**
 * _strcmp - compare les valeurs de chaîne
 * @s1 : valeur d'entrée
 * @s2 : valeur d'entrée
 *
 * Retour : s1[i] - s2[i]
 */
int _strcmp(car *s1, car *s2)
{
        int je ;
        je = 0 ;
        tandis que (s1[i] != '\0' && s2[i] != '\0')
        {
                si (s1[i] != s2[i])
                {
                        retour (s1[i] - s2[i]);
                }
        je++ ;
        }
        retour (0);
}
