#include "main.h"
#include <stddef.h>
/**
 * _strchr - Point d'entrée
 * @s : entrée
 * @c : entrée
 * Retour : Toujours 0 (Succès)
 */
char *_strchr(char *s, char c)
{
        int je = 0 ;
        pour (; s[i] >= '\0'; i++)
        {
                si (s[i] == c)
                        retour (&s[i]);
        }
        retour (0);
}
